{
  "name": "agent-room-workspace",
  "version": "1.0.9",
  "private": true,
  "scripts": {
    "test:npm": "node --test npm/*.test.cjs",
    "pack:npm": "node scripts/package-npm.mjs",
    "test:browser": "go build -ldflags \"-X agent_romm/internal/buildinfo.Version=1.0.9\" -o dist/dashboard-ui-fixture ./internal/dashboard/testdata/ui && playwright test"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@playwright/test": "1.58.2",
    "marked": "15.0.12"
  }
}
